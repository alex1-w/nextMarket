import styles from './Cart.module.scss';
import { FC } from "react"

const Cart: FC = () => {
    return (
        <div className={styles.main}>

        </div>
    )
};

export default Cart;
